# LLM 层剪枝 DQN 项目交接文档

## 1. 项目概述

本项目将 LLM 层剪枝建模为 MDP，并使用 Double Dueling DQN 学习在给定剪枝预算下的层删除顺序。目标是在减少 Transformer 层数的同时，尽量降低模型性能退化。

当前代码是 research prototype，已整理为自包含版本，包含核心训练、评测入口和项目内依赖。默认 WikiText2、BookCorpus、C4 等数据集只适合研究复现，不能代表业务效果。业务场景下必须重新设置校准集、验证集和测试集，并以二分类/排序相关业务指标为准评估剪枝效果。

## 2. 核心文件

| 文件 | 作用 |
| --- | --- |
| `train_dqn.py` | 主训练入口。多 GPU worker 采集 episode，主进程更新 DQN，并在训练结束后自动验证和评测。 |
| `eval_experiment.py` | 独立评测入口。读取实验目录中的 `config.json`，支持 single-budget 和 multi-budget 评测。 |
| `eval_sequence.py` | 给定剪枝序列，评估 PPL。 |
| `environment.py` | `LayerEnv`，定义层剪枝 MDP，包括状态、动作、action mask、奖励和 episode 终止条件。 |
| `dqn_agent.py` | `DQNAgent`，包含 Dueling/Transformer Q 网络、Replay Buffer 和 Double DQN 更新逻辑。 |
| `evaluation_pipeline.py` | 评测 pipeline，包括数据加载、checkpoint rollout、分布式 PPL 评测和报告写入。 |
| `llm_pruner/data_utils.py` | 数据集加载逻辑。业务组替换校准集、验证集、测试集时主要修改这里。 |
| `llm_pruner/llm_wrapper.py` | 模型加载、层特征提取、跳层剪枝、PPL 计算和 snapshot/restore。 |

## 3. 方法简介

本项目的核心思想是把层剪枝过程表示成一个序列决策问题。DQN agent 每一步选择剪掉一层，环境基于剪枝后的模型表现返回 reward，直到达到剪枝预算并执行 stop action。

### 3.1 状态空间 state

状态维度为：

$$
\mathrm{state\_dim}=N_{\mathrm{layer}}\times 8+4
$$

其中每层有 8 维特征，最后拼接 4 维全局 meta 特征。当前实现位于 `environment.py::_get_state()` 和 `llm_pruner/llm_wrapper.py::_real_features_and_ppl()`。

记第 $l$ 层输入 hidden states 为 $h_l^{\mathrm{in}}$，输出 hidden states 为 $h_l^{\mathrm{out}}$。这些张量形状可以理解为 $[B,T,H]$，其中 $B$ 是 batch size，$T$ 是 token 数，$H$ 是 hidden size。以下公式默认对 batch 和 token 维度取平均。

搜索相关性任务是二分类任务。每个样本是 query-doc pair，标签为 $y\in\{0,1\}$，模型输出相关类概率：

$$
p(q,d)=P(y=1\mid q,d)=\sigma(s(q,d))
$$

其中 $q$ 表示 query，$d$ 表示 doc，$s(q,d)$ 是模型输出的 logit，$\sigma$ 是 sigmoid 函数。

### 3.2 每层 state 特征

#### `cos_sim`

含义：第 $l$ 层输入和输出 hidden state 的 cosine 相似度。越接近 1，说明该层对表示方向改动越小，可能更容易被剪。

计算公式：

$$
\mathrm{cos\_sim}_l=
\frac{1}{BT}\sum_{b=1}^{B}\sum_{t=1}^{T}
\frac{\left\langle h_{l,b,t}^{\mathrm{in}},h_{l,b,t}^{\mathrm{out}}\right\rangle}
{\left\|h_{l,b,t}^{\mathrm{in}}\right\|_2\left\|h_{l,b,t}^{\mathrm{out}}\right\|_2+\epsilon}
$$

业务迁移：可直接迁移到 query-doc 二分类相关性模型。

#### `weight_l1`

含义：第 $l$ 层参数的 L1 范数，反映参数整体绝对规模。

原始 L1 范数：

$$
L1_l=\sum_{w\in\theta_l}|w|
$$

写入 state 前做 min-max 归一化：

$$
\mathrm{weight\_l1}_l=
\frac{L1_l-\min_j L1_j}{\max_j L1_j-\min_j L1_j+\epsilon}
$$

业务迁移：与任务无关，可直接迁移。

#### `weight_l2`

含义：第 $l$ 层参数的 L2 范数，反映参数能量规模。

原始 L2 范数：

$$
L2_l=\sqrt{\sum_{w\in\theta_l}w^2}
$$

写入 state 前做 min-max 归一化：

$$
\mathrm{weight\_l2}_l=
\frac{L2_l-\min_j L2_j}{\max_j L2_j-\min_j L2_j+\epsilon}
$$

业务迁移：与任务无关，可直接迁移。

#### `residual_ratio`

含义：第 $l$ 层 residual update 相对输入 hidden state 的幅度。越小表示该层改动越弱。

计算公式：

$$
\mathrm{residual\_ratio}_l=
\frac{1}{BT}\sum_{b=1}^{B}\sum_{t=1}^{T}
\frac{\left\|h_{l,b,t}^{\mathrm{out}}-h_{l,b,t}^{\mathrm{in}}\right\|_2}
{\left\|h_{l,b,t}^{\mathrm{in}}\right\|_2+\epsilon}
$$

业务迁移：可直接迁移。

#### `output_norm_ratio`

含义：第 $l$ 层输出 hidden state 相对输入 hidden state 的范数比例。

计算公式：

$$
\mathrm{output\_norm\_ratio}_l=
\frac{1}{BT}\sum_{b=1}^{B}\sum_{t=1}^{T}
\frac{\left\|h_{l,b,t}^{\mathrm{out}}\right\|_2}
{\left\|h_{l,b,t}^{\mathrm{in}}\right\|_2+\epsilon}
$$

业务迁移：可直接迁移。

#### `token_logit_kl`

含义：第 $l$ 层前后对下一 token 分布的影响。当前实现是 causal LM 视角，比较最后一个有效 token 位置的词表分布变化。

当前 LM 公式：

$$
P_l^{\mathrm{in}}=\mathrm{softmax}\left(W_{\mathrm{lm}}\,\mathrm{Norm}\left(h_{l,\mathrm{last}}^{\mathrm{in}}\right)+b_{\mathrm{lm}}\right)
$$

$$
P_l^{\mathrm{out}}=\mathrm{softmax}\left(W_{\mathrm{lm}}\,\mathrm{Norm}\left(h_{l,\mathrm{last}}^{\mathrm{out}}\right)+b_{\mathrm{lm}}\right)
$$

$$
\mathrm{token\_logit\_kl}_l=
\mathbb{E}_{b}\left[D_{\mathrm{KL}}\left(P_{l,b}^{\mathrm{out}}\;\|\;P_{l,b}^{\mathrm{in}}\right)\right]
$$

其中：

$$
D_{\mathrm{KL}}(Q\|P)=\sum_v Q_v\log\frac{Q_v}{P_v}
$$

业务迁移：搜索相关性是二分类任务，模型输出 sigmoid 分数，不应把 LM 的下一 token KL 当作最终业务指标。建议改成相关性概率的 Bernoulli KL。设：

$$
p_l^{\mathrm{in}}=\sigma\left(s_l^{\mathrm{in}}(q,d)\right),\qquad
p_l^{\mathrm{out}}=\sigma\left(s_l^{\mathrm{out}}(q,d)\right)
$$

则业务版 KL 可以定义为：

$$
\mathrm{score\_kl}_l=
D_{\mathrm{KL}}\left(\mathrm{Bernoulli}\left(p_l^{\mathrm{out}}\right)\;\|\;\mathrm{Bernoulli}\left(p_l^{\mathrm{in}}\right)\right)
$$

展开为：

$$
\mathrm{score\_kl}_l=
p_l^{\mathrm{out}}\log\frac{p_l^{\mathrm{out}}}{p_l^{\mathrm{in}}}
+\left(1-p_l^{\mathrm{out}}\right)
\log\frac{1-p_l^{\mathrm{out}}}{1-p_l^{\mathrm{in}}}
$$

#### `pred_entropy_delta`

含义：第 $l$ 层前后预测不确定性的变化。当前实现是 causal LM 下一 token 分布熵变化。

当前 LM 分布熵：

$$
H(P)=-\sum_v P_v\log P_v
$$

当前特征计算：

$$
\mathrm{pred\_entropy\_delta}_l=
\mathbb{E}_{b}\left[H\left(P_{l,b}^{\mathrm{out}}\right)-H\left(P_{l,b}^{\mathrm{in}}\right)\right]
$$

业务迁移：搜索相关性二分类任务中，建议改成 sigmoid/Bernoulli 熵变化。二分类概率 $p$ 的熵为：

$$
H_{\mathrm{bin}}(p)=-p\log p-(1-p)\log(1-p)
$$

业务版 entropy delta 可以定义为：

$$
\mathrm{score\_entropy\_delta}_l=
H_{\mathrm{bin}}\left(p_l^{\mathrm{out}}\right)-H_{\mathrm{bin}}\left(p_l^{\mathrm{in}}\right)
$$

如果某层显著增加相关性判断的不确定性，剪掉它可能风险更高。

#### `alive_flag`

含义：当前层是否仍未被剪掉。

计算公式：

$$
\mathrm{alive\_flag}_l=
\begin{cases}
1, & \text{layer }l\text{ still exists} \\
0, & \text{layer }l\text{ has been pruned}
\end{cases}
$$

业务迁移：直接迁移。DQN 需要知道哪些层已经不可再选。

### 3.3 全局 meta 特征

每个 state 最后拼接 4 个全局特征，用来告诉 DQN 当前 episode 进展和整体性能变化。

#### `prune_progress`

含义：当前已经剪了多少层，占目标 budget 的比例。

计算公式：

$$
\mathrm{prune\_progress}=\frac{N_{\mathrm{pruned}}}{\max(B_{\mathrm{prune}},1)}
$$

业务迁移：直接迁移。

#### `norm_ppl`

含义：当前 PPL 相对初始 PPL 的归一化信号。当前实现用反比形式，PPL 越低该值越高。

计算公式：

$$
\mathrm{norm\_ppl}=\frac{\mathrm{PPL}_{\mathrm{initial}}}{\max(\mathrm{PPL}_{\mathrm{current}},10^{-6})}
$$

业务迁移：如果改成业务二分类指标，可以替换为业务指标归一化。例如以 AUC 为主时：

$$
\mathrm{norm\_auc}=\frac{\mathrm{AUC}_{\mathrm{current}}}{\max(\mathrm{AUC}_{\mathrm{initial}},\epsilon)}
$$

如果以二分类 logloss/BCE 为主，loss 越低越好，可以使用：

$$
\mathrm{norm\_bce}=\frac{\mathrm{BCE}_{\mathrm{initial}}}{\max(\mathrm{BCE}_{\mathrm{current}},\epsilon)}
$$

#### `delta_ppl`

含义：上一步到当前 step 的性能变化。当前 PPL 上升时该值通常为负。

计算公式：

$$
\mathrm{delta\_ppl}=\frac{\mathrm{PPL}_{\mathrm{prev}}-\mathrm{PPL}_{\mathrm{current}}}{\max(\mathrm{PPL}_{\mathrm{prev}},1)}
$$

业务迁移：建议换成业务指标的 step-level delta。若使用二分类 BCE：

$$
\mathrm{delta\_bce}=\frac{\mathrm{BCE}_{\mathrm{prev}}-\mathrm{BCE}_{\mathrm{current}}}{\max(\mathrm{BCE}_{\mathrm{prev}},\epsilon)}
$$

若使用 AUC：

$$
\mathrm{delta\_auc}=\mathrm{AUC}_{\mathrm{current}}-\mathrm{AUC}_{\mathrm{prev}}
$$

#### `budget_ratio`

含义：当前目标剪枝 budget 占总层数比例。

计算公式：

$$
\mathrm{budget\_ratio}=\frac{B_{\mathrm{prune}}}{N_{\mathrm{layer}}}
$$

业务迁移：直接迁移。多 budget 训练时尤其重要，它让 agent 区分当前 episode 是轻剪枝还是重剪枝。

注意：当前代码里计算过剩余层比例，但没有拼入最终 state：

$$
\mathrm{remaining\_ratio}=\frac{N_{\mathrm{remaining}}}{N_{\mathrm{layer}}}
$$

最终实际使用的 meta 特征是上面 4 个。

### 3.4 迁移到搜索相关性二分类任务时的 state 改造建议

第一步，保持结构类和表示类特征不变：

- `cos_sim`
- `weight_l1`
- `weight_l2`
- `residual_ratio`
- `output_norm_ratio`
- `alive_flag`
- `prune_progress`
- `budget_ratio`

这些特征不强依赖 PPL，可以直接在业务 query-doc 样本上计算。

第二步，把 LM 分布特征替换为二分类相关性分数特征：

$$
p_l^{\mathrm{in}}=\sigma\left(s_l^{\mathrm{in}}(q,d)\right)
$$

$$
p_l^{\mathrm{out}}=\sigma\left(s_l^{\mathrm{out}}(q,d)\right)
$$

$$
\mathrm{score\_kl}_l=D_{\mathrm{KL}}\left(\mathrm{Bernoulli}\left(p_l^{\mathrm{out}}\right)\;\|\;\mathrm{Bernoulli}\left(p_l^{\mathrm{in}}\right)\right)
$$

$$
\mathrm{score\_entropy\_delta}_l=H_{\mathrm{bin}}\left(p_l^{\mathrm{out}}\right)-H_{\mathrm{bin}}\left(p_l^{\mathrm{in}}\right)
$$

第三步，把 PPL meta 和 reward 替换为二分类业务指标。二分类 BCE 为：

$$
\mathrm{BCE}(y,p)=-y\log p-(1-y)\log(1-p)
$$

可以用剪枝前后 BCE 的变化构造 reward：

$$
r_t^{\mathrm{biz}}=\mathrm{BCE}_{t-1}-\mathrm{BCE}_t
$$

如果希望剪枝后尽量贴近原模型，也可以用原模型 sigmoid 分数 $p^{\mathrm{teacher}}$ 作为 teacher score，计算 MSE：

$$
\mathrm{MSE}=\frac{1}{N}\sum_{i=1}^{N}\left(p_i^{\mathrm{pruned}}-p_i^{\mathrm{teacher}}\right)^2
$$

### 3.5 动作、奖励和训练算法

动作 `action`：

- `0..num_layers-1`：剪掉对应 Transformer layer。
- `num_layers`：stop action，表示结束当前剪枝 episode。

动作约束：

- 已剪掉的层会被 mask。
- 默认保护第 `0,1` 层，不允许剪枝。
- 在 `min_prune_budget` 用完之前，stop action 会被 mask。

当前 research reward：

$$
r_t=-\alpha\log\frac{\mathrm{PPL}_t}{\mathrm{PPL}_{t-1}}
$$

如果剪枝后 PPL 偶然下降，正 reward 会被 clamp 到 0，避免把噪声当成收益。如果 PPL 超过 clip 或 ratio threshold，会给 `ppl_penalty` 并终止 episode。迁移到业务二分类任务后，建议使用 BCE/logloss、AUC proxy 或 teacher score loss 的变化来构造 reward。

训练算法：

- Double DQN：用 online Q 网络选动作，用 target Q 网络计算 target。
- Dueling DQN：拆分 value head 和 advantage head。
- Replay Buffer：离策略学习，多 budget 时使用分桶采样。
- Epsilon-greedy：训练期探索，验证和评测期使用 deterministic rollout。

## 4. 运行环境

| 项目 | 当前验证情况 |
| --- | --- |
| GPU | 8 x NVIDIA A100-SXM4-80GB |
| PyTorch | `2.7.1+cu126` |
| Transformers | `4.55.4` |
| flash-attn | `2.8.3` |
| CUDA 可用性 | `torch.cuda.is_available() == True` |

主要 Python 依赖：

```text
torch
transformers
datasets
flash-attn
numpy
wandb
```

## 5. 数据集

当前 research 默认数据集是 WikiText2、BookCorpus、C4，适合验证代码链路和论文实验假设，但不适合作为业务结论依据。业务场景是搜索相关性二分类判断：输入包含一个 `query` 和一个 `doc`，模型输出一个 sigmoid 分数，表示该文档与 query 相关的概率。

因此，业务组需要重新构造数据：

| 数据类型 | 当前 research 默认 | 业务接手后应替换为 | 在流程中的作用 |
| --- | --- | --- | --- |
| 校准集 calibration | WikiText2 train 或 BookCorpus | 业务 query-doc 样本池 | 训练环境计算层特征、PPL 或业务 reward，给 DQN 提供剪层反馈 |
| 验证集 validation | WikiText2 validation 或 BookCorpus validation | 固定业务 holdout query-doc 集 | --- |
| 测试集 test | WikiText2 test / C4 validation | 最终业务 query-doc 测试集 | --- |

### 5.1 校准集 calibration

校准集是训练过程中最频繁使用的数据。每个 episode 中，环境会基于校准样本计算当前剪枝状态下的模型表现，并把这个变化转成 reward，DQN agent 根据 reward 学习下一层剪哪一层。

在当前 research 代码中，校准集主要用于：

- 计算每层特征，例如 hidden state 相似度、残差比例、logit KL 等。
- 计算剪枝前后的 PPL 变化。
- 根据 PPL ratio 构造 DQN reward。

在搜索相关性业务中，校准集应该改为业务 query-doc 样本。它的作用是让 agent 看到剪枝组合对特定任务输入分布的影响。

### 5.2 业务指标和当前 PPL 代码的差异

当前代码中的 `LLMWrapper.evaluate_ppl()` 是 causal LM 风格的 PPL 评测。它主要用于 research 任务，评估剪枝后模型的语言建模能力。但搜索相关性模型的核心效果是二分类相关概率，不能依赖 PPL 得出业务结论。

业务落地时建议做两步改造：

1. 在 `llm_wrapper.py` 中增加 query-doc relevance scoring 路径，输入 `query/doc`，输出 sigmoid 分数。
2. 在 `evaluation_pipeline.py` 中增加业务指标聚合，把 validation/test 的 checkpoint 选择逻辑从 PPL 变为业务指标。

## 6. 训练命令

### 6.1 单 budget

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 python train_dqn.py \
  --model Qwen/Qwen3-8B \
  --budget 8 \
  --num-gpus 4 \
  --num-iters 300 \
  --updates-per-iter 50 \
  --batch-size 64 \
  --cal-source business \
  --cal-pool-size 128 \
  --num-cal 8 \
  --cal-seq-len 512 \
  --val-interval 50 \
  --ckpt-interval 50 \
  --protected-layers "0,1" \
  --exp-name business/qwen3_8b_budget8_v1
```

### 6.2 多 budget

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7 python train_dqn.py \
  --model Qwen/Qwen3-8B \
  --budget 16 \
  --budget-candidates "4,8,12,16" \
  --num-gpus 8 \
  --num-iters 800 \
  --updates-per-iter 50 \
  --batch-size 64 \
  --cal-source business \
  --cal-pool-size 256 \
  --num-cal 8 \
  --cal-seq-len 512 \
  --val-interval 100 \
  --ckpt-interval 50 \
  --protected-layers "0,1" \
  --exp-name business/qwen3_8b_multibudget_v1
```

## 7. 评测命令

### 7.1 复评单 budget 实验

```bash
python eval_experiment.py \
  --exp-dir experiments/business/qwen3_8b_budget8_v1 \
  --ckpts 100,200,300,final \
  --eval-gpus 4
```

### 7.2 复评多 budget 实验

```bash
python eval_experiment.py \
  --exp-dir experiments/business/qwen3_8b_multibudget_v1 \
  --mode multi \
  --budgets "4,8,12,16" \
  --ckpts 200,400,final \
  --eval-gpus 8
```

### 7.3 评估人工或外部剪枝序列

```bash
python eval_sequence.py \
  --model Qwen/Qwen3-8B \
  --order "28,13,2,21" \
  --prune-counts "2,4" \
  --gpu 0 \
  --output manual_sequence_eval.json
```

## 8. 输出文件说明

典型实验目录：

```text
experiments/<exp_name>/
├── config.json
├── checkpoints/
│   ├── dqn_iter100.pt
│   ├── dqn_iter200.pt
│   └── dqn_final.pt
├── history.json
├── val_history.json
├── val_results.md
├── pruning_plans.json
├── eval_results.json
├── eval_results.md
└── pipeline_summary.json
```

| 文件 | 内容 | 业务组主要用途 |
| --- | --- | --- |
| `config.json` | 训练参数和数据参数 | 复现实验 |
| `history.json` | reward、PPL、loss、epsilon、stop ratio 等训练曲线 | 排查训练是否稳定 |
| `val_history.json` | 每次验证的剪枝顺序、验证 PPL、各 budget 结果 | 分析 checkpoint 选择 |
| `val_results.md` | 验证阶段 Markdown 报告 | 快速人工查看 |
| `pruning_plans.json` | 持久化剪枝顺序 | 后续复评或部署使用 |
| `eval_results.json` | 最终评测原始结果 | 自动化汇总 |
| `eval_results.md` | 最终评测报告 | 人工评审 |
| `pipeline_summary.json` | 最佳 checkpoint、最终报告路径 | 汇总入口 |

## 9. 业务落地建议流程

1. 业务数据集。作为calibration, validation, test。
2. 问题建模的State中相关特征的修改
3. 问题建模的reward计算改为业务相关的指标
