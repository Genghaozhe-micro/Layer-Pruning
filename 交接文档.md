# LLM 层剪枝 DQN 项目交接文档

## 1. 项目概述

本项目将 LLM 层剪枝建模为 MDP，并使用 Double Dueling DQN 学习在给定剪枝预算下的层删除顺序。目标是在减少 Transformer 层数的同时，尽量降低模型性能（PPL）退化。

代码已经整理为自包含版本，包含核心训练、评测入口和项目内依赖。

需要特别注意：当前代码仍是 research prototype。默认使用的 WikiText2、BookCorpus、C4 等数据集只适合研究复现，并且指标 PPL 不能直接代表业务效果。如果要在业务场景下使用，应当：
1. 重新设置校准集、验证集和测试集。
2. 定义场景下的性能指标（比如分类准确率）。

## 2. 代码位置与核心文件

```text
my_pure
├── 交接文档.md
├── train_dqn.py
├── eval_experiment.py
├── eval_sequence.py
├── evaluation_pipeline.py
├── environment.py
├── dqn_agent.py
└── llm_pruner/
    ├── __init__.py
    ├── data_utils.py
    └── llm_wrapper.py
```

核心文件说明：

| 文件 | 作用 |
| --- | --- |
| `train_dqn.py` | 主训练入口。多 GPU worker 采集 episode，主进程更新 DQN，并在训练结束后自动验证和最终评测。 |
| `eval_experiment.py` | 独立评测入口。读取实验目录中的 `config.json`，支持 single-budget 和 multi-budget 评测。 |
| `eval_sequence.py` | 给定剪枝序列，评估 PPL。 |
| `environment.py` | `LayerEnv`，定义层剪枝 MDP，包括状态、动作、action mask、奖励和 episode 终止条件。 |
| `dqn_agent.py` | `DQNAgent`，包含 Dueling/Transformer Q 网络、Replay Buffer 和 Double DQN 更新逻辑。 |
| `evaluation_pipeline.py` | 评测的 pipeline，包括数据加载、checkpoint rollout、分布式 PPL 评测和报告写入。 |
| `llm_pruner/data_utils.py` | 数据集加载逻辑。业务组替换校准集、验证集、测试集时主要修改这里。 |
| `llm_pruner/llm_wrapper.py` | 模型加载、层特征提取、跳层剪枝、PPL 计算和 snapshot/restore。 |

## 3. 方法简介

本项目的核心思想是把层剪枝过程表示成一个序列决策问题。

状态 `state`：

- 每层 8 维特征：`cos_sim`、`weight_l1`、`weight_l2`、`residual_ratio`、`output_norm_ratio`、`token_logit_kl`、`pred_entropy_delta`、`alive_flag`。
- 全局 4 维 meta 特征：剪枝进度、当前 PPL、上一 step 的 PPL 变化、budget 比例。

动作 `action`：

- `0..num_layers-1`：剪掉对应 Transformer layer。
- `num_layers`：stop action，表示结束当前剪枝 episode。

动作约束：

- 已剪掉的层会被 mask。
- 默认保护第 `0,1` 层，不允许剪枝。
- 在 `min_prune_budget` 用完之前，stop action 会被 mask。

奖励：

```text
reward = -alpha * log(curr_ppl / prev_ppl)
```

如果剪枝后 PPL 偶然下降，正奖励会被 clamp 到 0，避免把噪声当成收益。如果 PPL 超过 clip 或 ratio threshold，会给 `ppl_penalty` 并终止 episode。

训练算法：

- Double DQN：用 online Q 网络选动作，用 target Q 网络计算 target。
- Dueling DQN：拆分 value head 和 advantage head。
- Replay Buffer：离策略学习，多 budget 时使用分桶采样。
- Epsilon-greedy：训练期探索，验证和评测期使用 deterministic rollout。

## 5. 运行环境

| 项目 | 当前验证情况 |
| --- | --- |
| GPU | 8 x NVIDIA A100-SXM4-80GB |
| PyTorch | `2.7.1+cu126` |
| Transformers | `4.55.4` |
| flash-attn | `2.8.3` |
| CUDA 可用性 | `torch.cuda.is_available() == True` |

主要 Python 依赖：

```text
torch
transformers
datasets
flash-attn
numpy
wandb
```

## 6. 数据集

这一节是业务组接手时最重要的部分。

当前 research 默认数据集是 WikiText2、BookCorpus、C4，适合验证代码链路和论文实验假设，但不适合作为业务结论依据。业务场景是搜索相关性判断：输入包含一个 `query` 和一个 `doc`，模型输出一个 sigmoid 分数，表示该文档与 query 的相关性。

因此，业务组需要重新构造三类数据：

| 数据类型 | 当前 research 默认 | 业务接手后应替换为 | 在流程中的作用 |
| --- | --- | --- | --- |
| 校准集 / reward pool | WikiText2 train 或 BookCorpus | 业务 query-doc 样本池 | 训练环境计算层特征、PPL 或业务 reward，给 DQN 提供剪层反馈 |
| 验证集 validation | WikiText2 validation 或 BookCorpus validation | 固定业务 holdout query-doc 集 | -- |
| 测试集 test | WikiText2 test / C4 validation | 最终业务 query-doc 测试集 | -- |

### 6.1 校准集 calibration

校准集是训练过程中最频繁使用的数据。每个 episode 中，环境会基于校准样本计算当前剪枝状态下的模型表现，并把这个变化转成 reward，DQN agent 根据 reward 学习“下一层剪哪一层”。

在当前 research 代码中，校准集主要用于：

- 计算每层特征，例如 hidden state 相似度、残差比例、logit KL 等。
- 计算剪枝前后的 PPL 变化。
- 根据 PPL ratio 构造 DQN reward。

在搜索相关性业务中，校准集应该改为业务 query-doc 样本。它的作用是让 Agent 知道某个剪枝组合对模型性能在特定任务上的影响。

业务建议：

- 建议初期校准集样本数量在 `128-1024` 条左右；正式实验可根据算力扩大。由于一个episode的每个step都需要forward计算特征，所以当校准集太大的时候训练速度会很慢，但当校准集太小的时候训练噪音很大。
- 将 reward 中的 PPL 修改为业务特定的指标，比如分类准确率。

示例序列化格式：

```text
Query: {query}
Document: {doc}
Relevance score:
```

### 6.2 验证集 validation

验证集用于训练过程中的 checkpoint 选择。它不参与 episode 采样，也不应该影响 replay buffer；它的作用是回答“当前 DQN 学到的剪枝顺序，在业务 holdout 数据上是否更好”。

在当前 pipeline 中，验证集用于：

- 每隔 `val_interval` 轮运行 deterministic rollout。
- 记录不同 checkpoint 的剪枝顺序。
- 比较不同剪枝 budget 下的验证 PPL。
- 选择最终进入 final evaluation 的 checkpoint。

在搜索相关性业务中，validation 应该将 PPL 指标替换为业务指标。

### 6.3 测试集 test

测试集只用于最终报告和上线前评审。它不参与训练、不参与 reward 计算，也不参与 checkpoint 或超参选择。

在当前 pipeline 中，测试集用于：

- 对最终选出的 checkpoint 和剪枝顺序做一次最终 PPL 评估。
- 写入 `eval_results.json` 和 `eval_results.md`。

### 6.4 业务指标和当前 PPL 代码的差异

当前代码中的 `LLMWrapper.evaluate_ppl()` 是 causal LM 风格的 PPL 评测。它主要用于 research 任务，评估剪枝后模型的**语言建模能力**。但搜索相关性模型的效果不依赖 PPL。

业务落地时建议做两步改造：

1. 在 `llm_wrapper.py` 中增加 query-doc relevance scoring 路径，输入 `query/doc`，输出 sigmoid 分数。
2. 在 `evaluation_pipeline.py` 中增加业务指标聚合，把 validation/test 的 checkpoint 选择逻辑从 PPL 变为业务指标。

## 7. 如何替换业务数据

主要修改文件：

```text
my_pure/final/llm_pruner/data_utils.py
```

训练数据加载调用链：

```text
train_dqn.py
  -> evaluation_pipeline.load_pipeline_datasets()
  -> llm_pruner.data_utils.load_calibration()
  -> llm_pruner.data_utils.load_calibration_bookcorpus()
  -> llm_pruner.data_utils.load_wikitext2()
  -> llm_pruner.data_utils.load_c4_validation()
```

建议业务组新增一种数据源，例如：

```bash
--cal-source business
```

并补充以下参数或配置文件字段：

```text
--business-cal-path /path/to/business_calibration.jsonl
--business-val-path /path/to/business_validation.jsonl
--business-test-path /path/to/business_test.jsonl
```

## 8. 训练命令

### 8.1 单 budget 

业务数据源接好后，建议类似下面这样跑：

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3 python train_dqn.py \
  --model Qwen/Qwen3-8B \
  --budget 8 \
  --num-gpus 4 \
  --num-iters 300 \
  --updates-per-iter 50 \
  --batch-size 64 \
  --cal-source business \
  --cal-pool-size 128 \
  --num-cal 8 \
  --cal-seq-len 512 \
  --val-interval 50 \
  --ckpt-interval 50 \
  --protected-layers "0,1" \
  --exp-name business/qwen3_8b_budget8_v1
```

### 8.2 多 budget

```bash
CUDA_VISIBLE_DEVICES=0,1,2,3,4,5,6,7 python train_dqn.py \
  --model Qwen/Qwen3-8B \
  --budget 16 \
  --budget-candidates "4,8,12,16" \
  --num-gpus 8 \
  --num-iters 800 \
  --updates-per-iter 50 \
  --batch-size 64 \
  --cal-source business \
  --cal-pool-size 256 \
  --num-cal 8 \
  --cal-seq-len 512 \
  --val-interval 100 \
  --ckpt-interval 50 \
  --protected-layers "0,1" \
  --exp-name business/qwen3_8b_multibudget_v1
```

## 9. 评测命令

### 9.1 复评单 budget 实验

```bash
python eval_experiment.py \
  --exp-dir experiments/business/qwen3_8b_budget8_v1 \
  --ckpts 100,200,300,final \
  --eval-gpus 4
```

### 9.2 复评多 budget 实验

```bash
python eval_experiment.py \
  --exp-dir experiments/business/qwen3_8b_multibudget_v1 \
  --mode multi \
  --budgets "4,8,12,16" \
  --ckpts 200,400,final \
  --eval-gpus 8
```

### 9.3 评估指定剪枝序列

```bash
python eval_sequence.py \
  --model Qwen/Qwen3-8B \
  --order "28,13,2,21" \
  --prune-counts "2,4" \
  --gpu 0 \
  --output manual_sequence_eval.json
```

## 10. 输出文件说明

典型实验目录：

```text
experiments/<exp_name>/
├── config.json
├── checkpoints/
│   ├── dqn_iter100.pt
│   ├── dqn_iter200.pt
│   └── dqn_final.pt
├── history.json
├── val_history.json
├── val_results.md
├── pruning_plans.json
├── eval_results.json
├── eval_results.md
└── pipeline_summary.json
```

文件含义：

| 文件 | 内容 | 业务组主要用途 |
| --- | --- | --- |
| `config.json` | 训练参数和数据参数 | 复现实验 |
| `history.json` | reward、PPL、loss、epsilon、stop ratio 等训练曲线 | 排查训练是否稳定 |
| `val_history.json` | 每次验证的剪枝顺序、验证 PPL、各 budget 结果 | 分析 checkpoint 选择 |
| `val_results.md` | 验证阶段 Markdown 报告 | 快速人工查看 |
| `pruning_plans.json` | 持久化剪枝顺序 | 后续复评或部署使用 |
| `eval_results.json` | 最终评测原始结果 | 自动化汇总 |
| `eval_results.md` | 最终评测报告 | 人工评审 |
| `pipeline_summary.json` | 最佳 checkpoint、最终报告路径 | 汇总入口 |

最常看的文件：

```text
val_results.md
eval_results.md
pipeline_summary.json
```